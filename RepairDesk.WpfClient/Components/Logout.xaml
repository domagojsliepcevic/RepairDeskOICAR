<UserControl x:Class="RepairDesk.WpfClient.Components.Logout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RepairDesk.WpfClient.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" FontSize="50">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="1" Grid.Column="1" MinWidth="500">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!-- button -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Logout" Click="Logout_Click"
                        Background="#D10024" Foreground="White" Padding="5" FontWeight="Bold" Margin="5" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
